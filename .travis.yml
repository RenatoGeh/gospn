# Language
language: go

# Golang version.
go:
  - tip

# Dependencies
install:
  - sudo apt-get -qq update
  - go get -u github.com/gonum/stat
  - go get -u github.com/gonum/mathext

script: make build
